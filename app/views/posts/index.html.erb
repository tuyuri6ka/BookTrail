<div class="main posts-index">
    <div class="container">
 
        <% @posts.each do |post| %>
        <div class="posts-index-item">
        <table>
            <tr>
            <td><%= link_to(post.title,"/posts/#{post.id}") %></td>
            <td><%= link_to(post.author,"/posts/#{post.id}") %></td>
            </tr>
        </table>
        </div>
        <% end %>
    </div>

    </div>
</div>